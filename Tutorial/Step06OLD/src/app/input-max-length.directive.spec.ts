import { InputMaxLengthDirective } from './input-max-length.directive';

describe('InputMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
