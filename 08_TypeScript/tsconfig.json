// La présence d'un fichier tsconfig.json dans un répertoire
// indique que le répertoire est la racine d'un projet TypeScript.
// Le fichier tsconfig.json spécifie les fichiers racine et les options de compilation requises pour compiler le projet.
{
    "compilerOptions": {
        "outDir": "./out",
        "target": "es5",
        "pretty": true,
        "experimentalDecorators": true
    },
    "files": [
        "./main.ts",
        "types/scrollto.d.ts"
    ]
}